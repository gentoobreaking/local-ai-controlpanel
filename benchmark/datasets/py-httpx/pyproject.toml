[project]
name = "py-httpx"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["httpx"]
[project.optional-dependencies]
dev = ["pytest>=7"]
[tool.ruff]
line-length = 100
select = ["E9","F"]
