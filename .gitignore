# Rust 產物
src-tauri/target/
src-tauri/gen/

# Node 產物
node_modules/
dist/

# Tauri 打包產物
src-tauri/target/release/bundle/

# 圖示（需用 `pnpm tauri icon` 產生，見 README）
src-tauri/icons/

# 環境
.env
.env.local

# Control Plane 執行資料（SQLite；root-level 為執行狀態，不入庫；results-keep/ 內的例外）
.acp-data/
apps/control-plane/.acp-data/
/*.db
/*.db-shm
/*.db-wal

# Python / pytest / ruff 快取（T023 開始）
**/__pycache__/
**/.pytest_cache/
**/.ruff_cache/

# Artifact Controller 內部 scratch
**/.acp-cmp/
apps/control-plane/dist-bundle/

# 基準測試 + E2E 結果（T023+），執行狀態資料不入庫
results-keep/

tmp/
