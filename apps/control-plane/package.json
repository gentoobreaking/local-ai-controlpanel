{
  "name": "@acp/control-plane",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Agent Control Plane — Layer 6（spec §45.3：只 bind 127.0.0.1）",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "minimatch": "^10.0.1",
    "yaml": "^2.7.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
